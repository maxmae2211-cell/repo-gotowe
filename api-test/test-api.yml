---
execution:
  - executor: selenium
    scenario: ui_test

scenarios:
  ui_test:
    browser: Edge
    headless: true
    requests:
      - label: Open Google
        url: https://google.com

reporting:
  - module: final-stats
